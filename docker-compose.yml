version: '3.9'

services:
  user-dashboard:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: user-dashboard-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true